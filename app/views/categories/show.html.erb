
<%= render @category %>

<div class="d-inline-flex">
  <%= link_to "Edit this category", edit_category_path(@category), class: 'btn btn-sm btn-info mx-2' %>
  <%= link_to "Back to categories", categories_path, class: 'btn btn-sm btn-secondary mx-2' %>

  <%= button_to "Destroy this category", @category, method: :delete, class: 'btn btn-sm btn-danger mx-2' %>
</div>
